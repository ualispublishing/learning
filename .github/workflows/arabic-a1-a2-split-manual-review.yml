name: Arabic A1+A2 split manual review evidence

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]
    paths:
      - 'reading/tools/split_arabic_a1_a2_manual_review_evidence.py'
      - '.github/workflows/arabic-a1-a2-split-manual-review.yml'

permissions:
  contents: write

concurrency:
  group: arabic-a1-a2-split-manual-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  split:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
          persist-credentials: true
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Split evidence by unit
        run: python reading/tools/split_arabic_a1_a2_manual_review_evidence.py
      - name: Commit unit packets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reading/audit/arabic_a1_a2_manual_review_units
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Split Arabic A1/A2 manual review evidence [skip ci]"
          git fetch origin audit/arabic-a1-a2-integrated-2026-08-23
          git rebase origin/audit/arabic-a1-a2-integrated-2026-08-23
          git push origin HEAD:audit/arabic-a1-a2-integrated-2026-08-23
