name: Build Urdu top3000 continuation v15

on:
  workflow_dispatch:
  push:
    paths:
      - 'audit/opus_independent_urdu_lexicon.json'
      - 'audit/run_urdu_top3000_v15.request'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - name: Install tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install word2word wordfreq
      - name: Download dictionary sources
        run: |
          curl -L --fail --retry 5 --retry-all-errors 'https://raw.githubusercontent.com/rohan-c0de/ReadUrdu-Devanagari-English/main/data/dictionary.json' -o /tmp/readurdu-dictionary.json
          curl -L --fail --retry 5 --retry-all-errors 'https://kaikki.org/dictionary/Urdu/kaikki.org-dictionary-Urdu.jsonl' -o /tmp/kaikki-urdu.jsonl
      - name: Build dual-corpus candidate
        run: |
          python scripts/build_urdu_top3000_v15.py \
            --kaikki /tmp/kaikki-urdu.jsonl \
            --readurdu /tmp/readurdu-dictionary.json
      - name: Commit candidate evidence
        if: always()
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add audit/urdu_top3000_candidate_v15.csv audit/urdu_top3000_continuation_evidence_v15.csv audit/urdu_top3000_rejections_v15.csv audit/urdu_top3000_candidate_v15_summary.json || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Build dual-corpus Urdu top3000 v15 [skip ci]'
          for n in 1 2 3; do
            git fetch origin main
            git rebase --autostash origin/main
            if git push origin HEAD:main; then exit 0; fi
            sleep $((n*3))
          done
          exit 1
