name: Final Arabic audit pass 07 - CEFR difficulty calibration
on:
  workflow_dispatch:
  push:
    paths:
      - 'reading/tools/final_audit_arabic_pass07_cefr_difficulty_calibration.py'
      - 'reading/arabic/**/passages.jsonl'
      - 'reading/audit/final_review_refresh.request'
      - '.github/workflows/final-audit-arabic-pass07-cefr-difficulty-calibration.yml'
permissions:
  contents: write
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python reading/tools/final_audit_arabic_pass07_cefr_difficulty_calibration.py
      - name: Commit audit artifact
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add reading/audit/final_arabic_pass07_cefr_difficulty_calibration.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Record final Arabic audit pass 07 [skip ci]'
          git fetch origin main
          git rebase --autostash origin/main
          git push origin HEAD:main