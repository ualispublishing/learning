name: Arabic B1-C2 semantic triage

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]
    paths:
      - 'reading/tools/triage_arabic_b1_c2_semantic_candidates.py'
      - '.github/workflows/arabic-b1-c2-semantic-triage.yml'

permissions:
  contents: write

concurrency:
  group: arabic-b1-c2-semantic-triage-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  triage:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
          persist-credentials: true
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Triage grammar and discourse candidates
        run: python reading/tools/triage_arabic_b1_c2_semantic_candidates.py
      - name: Commit triage evidence
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reading/audit/arabic_b1_c2_semantic_triage_2026-08-23.json reading/audit/arabic_b1_c2_semantic_triage_summary_2026-08-23.md
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Triage Arabic B1-C2 grammar and discourse questions [skip ci]"
          git fetch origin audit/arabic-b1-c2-final-2026-08-23
          git rebase origin/audit/arabic-b1-c2-final-2026-08-23
          git push origin HEAD:audit/arabic-b1-c2-final-2026-08-23
