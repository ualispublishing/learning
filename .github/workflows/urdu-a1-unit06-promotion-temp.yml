name: TEMP Urdu A1 Unit 06 guarded promotion
on:
  push:
    branches: [promote/urdu-a1-unit06-2026-08-20]
permissions:
  contents: write
jobs:
  promote:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: promote/urdu-a1-unit06-2026-08-20
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Promote verified staged Unit 06
        run: python reading/tools/promote_urdu_a1_unit06.py
      - name: Commit canonical promotion and evidence
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add reading/urdu/a1/passages.jsonl reading/audit/urdu_a1_unit06_promotion_2026-08-20.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Promote Urdu A1 Unit 06 [skip ci]'
          git push origin HEAD:promote/urdu-a1-unit06-2026-08-20
