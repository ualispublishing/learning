# Trigger terminal Arabic A1-C2 generation-status synchronization.
name: Update generation status
on:
  workflow_dispatch:
  push:
    paths:
      - 'reading/tools/update_generation_status.py'
      - '.github/workflows/update-generation-status.yml'
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python reading/tools/update_generation_status.py
      - name: Commit synchronized generation status
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add reading/STATUS.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Synchronize reading generation status [skip ci]'
          git fetch origin main
          git rebase --autostash origin/main
          git push origin HEAD:main
