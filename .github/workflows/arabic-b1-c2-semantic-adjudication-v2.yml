name: Arabic B1-C2 semantic adjudication v2

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]
    paths:
      - 'reading/tools/adjudicate_arabic_b1_c2_semantics_v2.py'
      - '.github/workflows/arabic-b1-c2-semantic-adjudication-v2.yml'

permissions:
  contents: write

concurrency:
  group: arabic-b1-c2-semantic-adjudication-v2-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  adjudicate:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
          persist-credentials: true
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Adjudicate advanced grammar and discourse questions
        run: python reading/tools/adjudicate_arabic_b1_c2_semantics_v2.py
      - name: Commit semantic adjudication evidence
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reading/audit/arabic_b1_c2_semantic_adjudication_v2_2026-08-23.json reading/audit/arabic_b1_c2_semantic_adjudication_v2_summary_2026-08-23.md
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Adjudicate Arabic B1-C2 grammar and discourse semantics [skip ci]"
          git fetch origin audit/arabic-b1-c2-final-2026-08-23
          git rebase origin/audit/arabic-b1-c2-final-2026-08-23
          git push origin HEAD:audit/arabic-b1-c2-final-2026-08-23
