name: Sync French B2 Unit 07 Frontier
on:
  workflow_dispatch:
  push:
    paths:
      - 'reading/tools/sync_french_b2_unit07_frontier.py'
      - '.github/workflows/sync-french-b2-unit07-frontier.yml'
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python reading/tools/sync_french_b2_unit07_frontier.py
      - name: Commit synchronized frontier files
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add reading/STATUS.json reading/TASKS.md reading/AGENT_HANDOFF.md
          git diff --cached --quiet && exit 0
          git commit -m 'Advance French B2 frontier to Unit 08 [skip ci]'
          git fetch origin main
          git rebase --autostash origin/main
          git push origin HEAD:main
