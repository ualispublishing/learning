# Reproducibility trigger

This marker intentionally triggers the full language-workbook v1 release workflow after the inspected artifact from run 32793395397 was published to the release branch.

The build must reproduce the repaired Arabic, French, and Urdu decks, pass the 6,000-row fail-closed adjudication gates, pronunciation audit, all three PDF render passes, the 42-PDF structural/render preflight, and the final cross-gate release audit before publishing regenerated outputs.
