# Workbook v1.0 quality rebuild

The first generated artifact was rejected during linguistic inspection. This marker belongs to the corrected quality-review branch and documents that merge is blocked until the v2 corpus-quality workflow passes and the replacement PDF artifact is inspected.
