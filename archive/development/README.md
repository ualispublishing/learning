# Archived development iterations

Older build scripts and workflows are preserved here for provenance and reproducibility. They are not part of the active automation surface.

The current Urdu continuation maintenance path uses the `public_v8` builder/validator that remains under `scripts/` and `.github/workflows/`.
