# Archive

Historical material is stored here for provenance and possible future reference. Files in this directory are not part of the current learning state unless explicitly restored.

- `flashcards/` contains paused review material.
- `legacy_language_decks/` and `wordlists/` contain earlier language data.
- `development/` contains older build scripts and workflows kept for reproducibility.
